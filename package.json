{
  "name": "plane-war",
  "version": "1.0.0",
  "description": "飞机大战 - 纯 JavaScript + Vite",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5000 --host 0.0.0.0",
    "build": "vite build",
    "start": "vite preview --port 5000 --host 0.0.0.0",
    "lint": "eslint src/",
    "lint:all": "eslint src/",
    "ts-check": "echo 'no ts-check needed (pure JS project)'"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.4.1",
    "vite": "^6.0.0"
  }
}
